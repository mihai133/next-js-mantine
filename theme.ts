'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: 'Roboto, sans-serif',
  // fontSizes: { xs: '1.2rem', sm: '1.4rem', md: '1.6rem', lg: '1.8rem', xl: '2.0rem' },
});
